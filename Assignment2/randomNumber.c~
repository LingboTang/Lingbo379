#include <time.h>
#include <stdlib.h>
#include <stdio.h>

int main() {
	srand(time(NULL));
	int r;
	int M = 1;
	int N = 4;
    
    r = rand();
	r = r % (4 - 1 + 1) + 1;
	int counter = 0;
	for (int i =0; i<1000; i++)
	{
		if (r==1)
		{
			printf("number is \n%d\n",r);
			counter++;
		}
	}
	printf("Total 1 is: %d\n",counter);
	return 0;
}
